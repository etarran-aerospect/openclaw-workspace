services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: conway
      POSTGRES_PASSWORD: conway
      POSTGRES_DB: conway_native
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
