# Memory - 2026-04-16

## Solana Sniper Bot (v2) Progress
- **Issue Diagnosis**: Addressed user reports of the bot "not doing anything" by auditing logs and filters.
- **Improved Visibility**: Restored and enhanced logging in `main.py`. The bot now explicitly prints an `IDLE` status and granular rejection stats (reasons for skipping candidates) every discovery cycle.
- **Filter Optimization**: Significantly relaxed `pre_migration_filters` and `gmgn_filters` to increase trading frequency.
- **Bug Fixes**: Corrected a `SyntaxError` in `main.py` related to `try/except` indentation and fixed the `seen_tokens` logic to allow re-entry on promising probes.
- **Strategy Update**: Switched `tp_mode` to `fixed` (20%) in `config.yaml` to improve exit reliability in volatile conditions.

## Technical Environment
- Checked `positions.json` and `trades.json` to confirm no active trades were stuck.
- Verified `npx codeburn` usage for cost tracking is documented in `TOOLS.md`.
