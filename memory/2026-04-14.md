# 2026-04-14

- Built out two separate paper-trading projects: `solana-sniper-v2` for Solana token sniping and `prediction-bot-v1` for Polymarket short-horizon crypto markets.
- Solana bot direction is now clear: discovery quality is the bottleneck, with the target explicitly narrowed to fresh Pump.fun / Bonk / LetsBonk graduates rather than generic new Solana tokens.
- Current Solana discovery stack supports `onchain` Raydium polling with automatic DexScreener fallback, but public Solana mainnet RPC blocks the needed `getProgramAccounts` query for serious Raydium scanning. A better RPC provider will be needed if that path continues.
- GMGN was identified as the strongest next discovery source for fresh Solana graduates. Relevant lead: GMGN New Pair docs and likely websocket-based new-pool feed; direct site fetches were blocked by Cloudflare, so payload verification is still pending.
- Solana bot runtime/UX improvements completed: config-driven filters, hybrid TP mode with break-even after TP, stale exits, persistent paper positions/trades/PnL, startup settings output, discovery/rejection diagnostics, closed-trade logs, and backward-compatible position hydration.
- Prediction bot direction is also narrowed: stay focused on Polymarket crypto `5M`-style markets, not sports or broad event discovery. Generic Gamma polling did not expose the desired short-horizon board cleanly, so discovery needs to be reverse-mapped from the exact `/crypto/5M` surface.
- Important user preference reaffirmed across the work: keep both bots separate, paper-only for now, practical/production-usable over toy demos, with visible runtime diagnostics and config-adjustable behavior.

- Session context: Continuing work on the `solana-sniper-v2` bot, specifically debugging entry starvation in `pre_migration_ladder` mode.
- Implementation: Adding raw candidate sample logging to `main.py` and `gmgn_discovery.py` to compare incoming API data against current filter thresholds.
- Technical detail: Fixed an issue where `_append_example` in `main.py` was hitting duplicate text collisions during file edits.
- Status: Patching the discovery loop to capture the first 3 tokens from every bucket regardless of filter status to verify feed quality.
