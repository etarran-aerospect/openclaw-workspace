# Memory — 2026-04-01

## Vineyard Irrigation Leak Detection — Session Progress

### Tools written/updated today
- `reports/vineyard_row_zone_builder.py` — builds canopy corridor + interrow zone polygons from row centerlines + block. Tested OK on Borden data.
- `reports/vineyard_clean_vine_mask_tool.py` — builds clean binary vine mask from multispectral raster + row geometry. Confirmed workflow runs; currently debugging mask quality.

### Clean vine mask tool status
- Sieve in-place file-lock bug fixed (now writes to separate temp file)
- Updated to ask for **full canopy width** (internally divides by 2 for buffer)
- MSAVI2 added as index option; made default (better for early-season vine masking)
- Band order confirmed: B=1, G=2, R=3, NIR=4, RedEdge=5
- Default values updated to match actual band order
- **Current problem**: canopy corridor constraint is NOT cleaning interrow vegetation effectively — too much interrow vegetation remains in the final clean mask. Row-corridor intersection is not improving on raw MSAVI2 mask.

### Suspected root cause
- Canopy corridor width of 3.35 m may be too close to row spacing — corridors may be overlapping and covering too much of the interrow area, meaning the "constraint" has no effect.
- The raster intersection logic may also be failing silently (e.g. `(raw@1 = 1) AND (canopy@1 = 1)` returning all zeros or passing through the raw mask without any constraint).
- Need to verify: is the canopy corridor raster actually valid and geometrically correct? Is it visually constraining the corridor at all?

### Next steps (as of last message)
1. Debug why canopy corridor is not constraining the mask — check corridor raster visually
2. Consider lowering canopy width to see if too-wide corridor is the issue
3. May need to lower MSAVI2 threshold significantly (currently 0.30) to reduce interrow vegetation in the raw mask before row constraint is applied
4. Alternatively: try a tighter spectral threshold to knock down interrow pixels first, then apply row constraint

### Key technical decisions
- MSAVI2 preferred over NDVI/NDRE for early-season (reduces soil background influence)
- Row corridor constraint should use actual canopy width, not arbitrary narrow strip
- Sieve cleanup: write to separate temp file, not in-place
- Full canopy width parameter = more intuitive for user (internally halved for buffer)

### Vineyard data paths (Borden)
- Thermal: `H:/2025_05_25_Borden_thermal/2025_05_25_Block_456-thermal-ortho-metashape-clipped-top.tif`
- Multispectral: `H:/2025_05_24_Borden_Multispectral/2025_05_24-Multispectral-top.tif`
- Block polygon: `C:/Users/seanc/mask_2.gpkg`
- Row centerlines: in-memory from canonical row builder, EPSG:32610
