- Deployment/source-of-truth notes for AeroSpect websites:
  - Main production website: `https://aerospectinc.com`
    - Host: IONOS static hosting
    - Deploy method: manual upload of built static files
    - Current known live fingerprint (Mar 4 baseline): `/assets/index-ChH_nNva.js` and `/assets/index-CfGdpg2d.css`
    - Treat this fingerprint as the source-of-truth when identifying the currently deployed build.
    - Do NOT assume `~/aerospect` or any Vercel project matches production unless the built output/fingerprints match.
  - BVLOS production page: `https://bvlos.aerospectinc.com`
    - Host: Vercel
    - Connected Git repo: `etarran-aerospect/aerospect`
    - BVLOS page edits belong in the `aerospect` repo, not in IONOS.
  - Vercel project notes:
    - `aerospect-simplified` is connected to `etarran-aerospect/aerospect`; its correct output directory is `dist` (not `public`).
    - `aerospect-website-new-manus` is a separate Manus repo/draft line and must NOT be assumed to match main production without proof.
- Safe workflow rules for future website edits:
  - For main-site changes (`aerospectinc.com`): backup first, identify the exact source repo/version, preview locally, then upload `index.html` + matching `assets/*` together as a set.
  - For BVLOS changes: edit `etarran-aerospect/aerospect`, let Vercel redeploy, verify on `bvlos.aerospectinc.com`.
  - Red-line rule: do not manually upload a `dist/` bundle to IONOS unless local preview is correct and it is proven to be the intended production codebase.
- BVLOS content-only recovery notes (2026-03-22):
  - The correct visual baseline for `bvlos.aerospectinc.com` was confirmed as deployed commit `e74b9b5` in `etarran-aerospect/aerospect`.
  - Safe preview branch created from that exact baseline: `feat/bvlos-content-only-e74b9b5`.
  - User requirement: preserve deployed visuals exactly; only additive content/SEO changes are allowed unless explicitly requested.
  - Content additions requested/kept on the clean branch: FAQ section, FAQ JSON-LD schema, Common BVLOS Use Cases, wave dividers around the new sections, and collapsible FAQ answers.
  - Copy constraint: do not condense or rewrite page content unless Ethan explicitly asks; make only the requested textual substitutions.
  - Product/copy constraint: remove thermal and multispectral language from the BVLOS page for now; those belong on a separate future services/capability page.
  - Hero CTA lesson: verify the actual branch file and actual deployment source commit before claiming a change is present. During this session, earlier claims about CTA removal were wrong until the button was truly removed from `src/pages/BvlosPage.tsx` and pushed in commit `097f8de` (`Remove redundant BVLOS specialist hero button`).
  - Vercel verification rule: when a preview does not match expectation, check the deployment's Source commit first; stale previews can still point at older commits (e.g. `adedfd7`) even when newer code exists.
  - Future SEO/info architecture direction discussed with Ethan: create a lean `/services` hub page for internal linking and future thermal / monitoring / survey pages, while keeping BVLOS focused on BVLOS only.
