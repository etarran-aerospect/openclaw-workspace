# 2026-03-30

## Vineyard irrigation leak detection toolbox progress
- Reconstructed the intended QGIS toolbox chain for vineyard irrigation leak detection:
  1. Vineyard Row Builder
  2. Vineyard Vegetation Index Tool
  3. Vineyard Row Leak Detection Tool
- Confirmed the project had drifted because the leak-detection side became too output-heavy and because thermal/coregistration problems had to be solved first.
- Recovered / reviewed prior row builder variants and vegetation index tool from Telegram-shared files.

## New / updated scripts created today
- Wrote a simplified row-based leak screening tool:
  - `reports/vineyard_row_leak_detection_tool.py`
- Wrote a cleaned canonical one-reference production row builder:
  - `reports/vineyard_row_builder_production_canonical.py`

## Current state of the leak tool
- Leak tool is now functioning far enough to:
  - align vegetation mask to thermal raster grid automatically
  - create non-vine thermal
  - buffer rows
  - run zonal mean temperature stats
  - compute q20 threshold and field mean
  - output row screening polygons with fields like `nz_mean`, `q20`, `field_mean`, `delta_mean`, `flag`, `rank_cool`, `pct_rank`
- Confirmed successful run on Borden thermal example with computed values visible in QGIS logs, including q20 and field mean.
- Remaining issue is minor/polish-level: QGIS still reports a bogus missing optional `OUTPUT_NONVINE.tif` when `SAVE_NONVINE=False`, but the main row output succeeds.

## Interpretation / presentation direction
- Ethan wants the workflow to be explainable visually, not just polygons over a map.
- Recommended tomorrow's focus:
  - create intermediate explanatory layers / images like the paper:
    - full thermal
    - vine-only thermal
    - non-vine / soil-only thermal
    - final flagged row screening layer
  - build a paper-style figure / map sequence showing how the process works
- Better storytelling is needed so vineyard managers can see how the result was derived.

## Workflow / toolbox decisions
- Canonical row builder direction:
  - one reference row is enough
  - reference row geometry controls offsets
  - block boundary must not guide row direction/shape
  - block used only for filtering/clipping
  - projected CRS / meters required for production use
- Leak tool should stay simplified for now:
  - non-vine thermal
  - q20 threshold
  - row-based screening
  - minimal output clutter

## Likely next steps tomorrow
1. Patch / polish optional non-vine output behavior if needed.
2. Create or script intermediate thermal split layers for presentation.
3. Build a manager-friendly figure/layout showing:
   - RGB or orthomosaic
   - full thermal
   - vine-only thermal
   - non-vine thermal
   - flagged rows / top-ranked rows
4. Use one vineyard/date as the hero example first instead of trying to generalize immediately.
