# 2026-03-03

## Website finalization progress (aerospect-website-new-manus)
- Working file: `/home/ethantarran/code/aerospect-website-new-manus/client/src/pages/Home.tsx`.
- Contact section was redesigned to a split layout with image + content; new contact image added at `client/public/images/contact-vineyard.jpg`.
- Contact details updated to:
  - Phone: `707.619.6961` (tel link `+17076196961`)
  - Email: `hello@aerospectinc.com`
  - CTA mailto also updated to `hello@aerospectinc.com`.
- Header/nav cleanup completed:
  - Removed duplicate "How It Works" entries.
  - Nav order now: `What We Deliver`, `How It Works`, `Demo`, `Seasonal Services`, `Contact`.
  - Anchors normalized: `#approach`, `#how-it-works`, `#ndvi-demo`, `#seasons`, `#contact`.
- NDVI demo section copy updated to:
  - "Aerial imaging reveals variability, stress, and emerging risk patterns across your vineyard..."
- How It Works section upgraded per user direction:
  - Reduced vertical spacing.
  - Added outcomes under each step.
  - Added credibility chips: `<24h turnaround`, `Block-level precision`, `RGB + Thermal + Multispectral`.
  - Added section CTAs: `See Sample Report` and `Request Assessment`.
- Replaced the single How It Works visual with 3 labeled blocks using user-provided images:
  - `client/public/images/how-rgb.jpg`
  - `client/public/images/how-ndvi.jpg`
  - `client/public/images/how-thermal.jpg`

## Debugging/browser relay lessons
- Chrome Relay warning banner behavior can affect more than one tab/profile context; practical mitigation is isolating automation in a dedicated Chromium/Chrome instance.
- Created local launcher for isolated relay browser:
  - Script: `~/.local/bin/openclaw-relay-chrome`
  - Desktop entry: `~/.local/share/applications/openclaw-relay-chrome.desktop`
  - Profile dir: `~/.openclaw/relay-chrome-profile`
- Extension auth mismatch caused repeated Relay `!` state when wrong extension build/options were used.
- Installed correct packaged extension path via CLI:
  - `~/.openclaw/browser/chrome-extension`
- Local gateway auth was changed from token mode to none (loopback-only) to reduce extension token friction:
  - File edited: `~/.openclaw/openclaw.json`
  - `gateway.auth.mode` set to `none`.

## Process lesson
- User strongly prefers direct execution and minimal friction over tooling detours; when browser relay causes setup drag, continue fast iteration via direct code edits + local refresh loop.
