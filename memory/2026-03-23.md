- AeroSpect deployment source-of-truth note to follow strictly:
  - Main website currently deployed: `https://aerospectinc.com` / `https://www.aerospectinc.com`
    - Host/platform: IONOS static hosting
    - Deployment style: manual upload of built files
    - Current confirmed live build date reference: Mar 4
    - Exact live build fingerprint/source-of-truth: `index-ChH_nNva.js` and `index-CfGdpg2d.css`
    - Live source also contains `manus-runtime`
    - Rule: if a local repo/build does not match that fingerprint, it is NOT the current deployed main site and must not be used as the homepage baseline.
  - BVLOS site currently deployed: `https://bvlos.aerospectinc.com`
    - Host/platform: Vercel
    - Repo: `etarran-aerospect/aerospect`
    - Rule: BVLOS work belongs to the `aerospect` repo/Vercel flow, not the IONOS main site flow.
  - Other Vercel projects should be treated as drafts/sandboxes unless explicitly proven otherwise:
    - `aerospect-simplified` → connected to `etarran-aerospect/aerospect`, draft/test deployment history, output dir confusion (`public` vs `dist`)
    - `aerospect` Vercel project variants → experiments/deployment targets, not automatically the live main site
    - `aerospect-website-new-manus` → separate draft/site line from Manus, not the current deployed main site by default
  - Operational rule for future homepage edits:
    - Main-site edits must start from the IONOS deployed baseline/fingerprint (`index-ChH_nNva.js` + `index-CfGdpg2d.css`), not from the old Vercel app/homepage unless proven identical.
    - Do not treat the `aerospect` repo preview homepage as the live `www.aerospectinc.com` homepage baseline unless it is explicitly verified to match the IONOS deployed site.
- AeroSpect clean deployment map/checklist to follow:
  - Main production website
    - Domain: `https://aerospectinc.com`
    - Host: IONOS
    - Deploy method: manual upload of built static files
    - Current known live fingerprint (Mar 4 build): `/assets/index-ChH_nNva.js` and `/assets/index-CfGdpg2d.css`
    - Rule: if a local build does not correspond to that production baseline, do not upload it.
  - BVLOS production page
    - Domain: `https://bvlos.aerospectinc.com`
    - Host: Vercel
    - Repo: `etarran-aerospect/aerospect`
    - Rule: BVLOS page edits happen in the `aerospect` repo, not in IONOS.
  - Draft / sandbox projects
    - `aerospect-simplified` → Vercel project tied to `etarran-aerospect/aerospect`, likely draft/test, output dir should be `dist` if kept
    - `aerospect-website-new-manus` → separate Manus repo/draft line; do not assume it matches main production unless proven
- Safe workflow checklist:
  - For main site changes (`aerospectinc.com`):
    1. Backup current IONOS site
    2. Identify the exact source repo/version
    3. Build locally
    4. Preview locally
    5. Confirm the build matches expected visuals
    6. Upload `index.html` + matching `assets/*` as a set
    7. Verify in incognito
  - For BVLOS page changes (`bvlos.aerospectinc.com`):
    1. Edit repo `etarran-aerospect/aerospect`
    2. Push branch / PR
    3. Let Vercel redeploy
    4. Verify on subdomain
- Red-line rules:
  - Do not use `~/aerospect` builds for the main site unless Ethan explicitly wants to replace the main site with that version.
  - Do not upload random `dist/` folders to IONOS unless local preview is correct.
  - Do not assume a Vercel project equals production.
  - For the main site, truth is what `aerospectinc.com` source shows and what files are actually in IONOS.
- Fast verification reminders:
  - Main site fingerprint check: inspect live source for `/assets/index-*.js` and `/assets/index-*.css`
  - BVLOS source check: Vercel project with domain `bvlos.aerospectinc.com` → Settings → Git
  - Local build upload readiness: preview looks correct and `index.html` references asset files that exist in `assets/`
- Request routing reminder:
  - “Update BVLOS page” → work in `aerospect` repo only
  - “Add something to main site nav/homepage” → first verify the actual source repo/version for `aerospectinc.com`, then build/preview/upload
- Main-site homepage progress checkpoint (2026-03-23 evening):
  - Ethan loaded a likely actual deployed-site source into workspace at:
    - `/home/ethan/.openclaw/workspace/aerospect-deployed-site/aerospect`
  - This is the first promising source folder that appears to contain the actual editable homepage source for the deployed main site, instead of the wrong Vercel app baseline.
  - Confirmed in built code from this folder:
    - target section id: `#how-it-works`
    - target section text: `From flight to recommendation, we turn aerial data into practical vineyard decisions.`
    - exact image to replace in that section: `/images/drone-inspection.jpg`
  - Ethan's requested change is now pinned down exactly:
    - On the main deployed homepage only, in the existing `How It Works` section, replace the single wide image (`drone-inspection.jpg`) with 3 images in this order:
      1. NDVI
      2. Thermal
      3. RGB with vine overlay
    - Keep everything else unchanged.
    - Preview only first; do not change live site until Ethan approves.
  - Important: do NOT use the old Vercel `aerospect` app homepage as the baseline for this task. The correct working source for tomorrow should start from `aerospect-deployed-site/aerospect`.
  - Next step for tomorrow:
    - locate the actual editable source file in `aerospect-deployed-site/aerospect` that renders `#how-it-works`
    - make only the one image-block swap there
    - preview safely before any deployment discussion
