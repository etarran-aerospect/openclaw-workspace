# 2026-03-01

## Website refresh (AeroSpect)
- Repo used for marketing refresh: https://github.com/etarran-aerospect/aerospect-simplified
- Branch pushed with first-pass homepage updates: `refresh-marketing-site-2026-02-28`
  - Hero headline/intro copy tightened ("Actionable Vineyard Intelligence…")
  - Contact updated to phone (707) 819-6961 and email hello@aerospectinc.com
  - Footer year updated to 2026
  - Umami analytics script made optional (only loads if VITE_ANALYTICS_* env vars set)
- Ethan viewed Vercel Deployments and confirmed Preview deployment for that branch is Ready; domain still points to Google Sites until DNS cutover.
- Vercel setting to verify for correct static output: Output Directory should be `dist/public` (Build & Deployment settings).

## ACP enablement (OpenClaw)
- Enabled ACP in OpenClaw config (`acp.enabled=true`, `acp.dispatch.enabled=true`) and restarted gateway via config apply.
- `openclaw doctor --non-interactive` highlights:
  - 3 orphan transcript files under `~/.openclaw/agents/main/sessions`
  - 1 live session lock file (not stale)
  - Memory search enabled but no embedding provider/API key configured.

## Notes
- Untracked nested repo `aerospect-website/` was cloned into the OpenClaw workspace; added to workspace `.gitignore` to avoid committing nested repos.

## Session summary (Ethan)
- Major execution failure on Canva + Google Sites editing tasks; user reported repeated wasted time and low confidence.
- We attempted content edits in Canva and later Google Sites. Changes were made in a draft context (`New AeroSpect`) but user validated against a different/old site context and did not see expected results.
- Mobile viewport testing (390x844) caused additional confusion in editor rendering.
- User ended session frustrated and requested state to be saved and synced to GitHub.

## What was requested/expected
- Direct execution with clear, visible results in the correct site.
- No more ambiguous progress claims.

## Current state to resume from
- Do **not** continue broad edits.
- On next attempt, require exact target Google Sites edit URL first, then do proof-first edits only:
  1) headline,
  2) hero subhead,
  3) seasonal services block,
  with screenshot proof before any further changes.

## User sentiment
- Trust significantly reduced due to repeated mismatches between reported and visible outcomes.
