# 2026-07-01

- Ethan is setting up a second OpenClaw agent on Hermes using an Oracle VPS. Local desktop OpenClaw is healthy (`2026.5.19`, Telegram OK, main agent active), but `ssh hermes` from WSL cannot resolve the hostname yet. Need actual VPS IP/DNS or SSH config before inspecting remote directly.
- Hermes Oracle VPS address provided: `ubuntu@129.146.53.190`. Host answers SSH, but desktop key is not authorized yet: `Permission denied (publickey)`. Local public key fingerprint: `SHA256:/7H8RdQPypbfWVHOEea9ek53lp9p8G6xreX84dApciw` (`~/.ssh/id_ed25519.pub`, comment `ethantarran@gmail.com`).
- Found the Oracle-generated private key at `/home/ethan/.openclaw/workspace/ssh-key-2026-07-02.key`; SSH originally rejected it due to loose `0644` permissions. Set it to `0600`, added `Host hermes` to `/home/ethan/.ssh/config`, and verified `ssh hermes` connects as `ubuntu` to hostname `hermes`.
- Ethan confirmed from WSL that `ssh hermes` now lands at `ubuntu@hermes:~$` on Ubuntu 24.04.4 Oracle aarch64. Reminder for future setup steps: once the prompt is `ubuntu@hermes`, do not run the desktop-only `ssh -i ~/.openclaw/workspace/ssh-key-2026-07-02.key ...` command again; proceed with remote install/config commands directly on Hermes.
- Correction: Ethan meant Nous Research Hermes Agent (`hermes-agent.nousresearch.com`, `NousResearch/hermes-agent`), not OpenClaw-on-a-server. The VM nickname `hermes` and Hermes Agent are related only because the server was named for it. Use official Hermes install commands, not `npm install -g openclaw`.
- Read-only Hermes VPS audit: `ssh hermes` works; host is Ubuntu 24.04.4 LTS on Oracle aarch64/arm64, ~11 GiB RAM, 45 GB root disk with ~6% used, passwordless sudo available for `ubuntu`, SSH key auth enabled and password auth disabled. Public probe showed only port 22 open among 22/80/111/443/3000/5173/8000/8080. Host firewall allows SSH and rejects other IPv4 inbound. Hermes Agent is not installed yet; `node`, `npm`, `uv`, `python3.11`, `build-essential`, `ripgrep`, and `ffmpeg` are not installed. Official Hermes installer is designed to install managed uv, Python 3.11, Node 22, and browser deps on Linux arm64.
- Checked Oracle VPS boot volume after Ethan asked if it was configured: current VM has `/dev/sda` 50.0GB disk, root `/dev/sda1` 45.6GB ext4 mounted at `/`, 42GB free. This means the custom 200GB boot volume from the Hermes guide was not set during creation. Instance is usable, but to match guide, resize boot volume in OCI to 200GB and grow partition/filesystem inside Ubuntu afterward.
- Rechecked Hermes Oracle VPS after console resize: Linux initially still saw the old 46.6GB disk, then a safe SCSI rescan exposed the resized 200GB boot disk. Ran `sudo growpart /dev/sda 1` and `sudo resize2fs /dev/sda1`; final state is `/dev/sda` 200G, `/dev/sda1` 199G, `/` ext4 193G total with 191G free. Public probe still shows only SSH open among 22/80/111/443/3000/5173/8000/8080.
