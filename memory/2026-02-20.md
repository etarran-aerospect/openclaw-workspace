## 2026-02-20 (PST) — Automaton setup notes

- Ethan set up Conway Research Automaton on this machine as a new instance; wants to reuse existing wallet/sandbox/API key but start fresh.
- Automaton was looping asking for an intake template. CLI checks used:
  - `node packages/cli/dist/index.js status`
  - `node packages/cli/dist/index.js logs --tail N`
- Attempt to message the automaton via CLI failed:
  - `node packages/cli/dist/index.js send <addr> "ping"` → `Failed to send message: fetch failed`
  - Root cause: social relay default host `https://social.conway.tech` is **NXDOMAIN** (confirmed via Cloudflare DoH JSON: Status=3).
  - Curl works for `https://conway.tech`, `https://app.conway.tech`, `https://api.conway.tech`, but `social.conway.tech` does not resolve.
  - Overriding `SOCIAL_RELAY_URL` did not take effect because send.js uses `config?.socialRelayUrl || process.env.SOCIAL_RELAY_URL || default`, and config likely populates a default `socialRelayUrl`.
  - `https://api.conway.tech/v1/messages` returns 404, so relay is not there.
- Local config/state found in `~/.automaton/`:
  - `automaton.json` contains name, creatorAddress, conwayApiKey/url, openaiApiKey, genesisPrompt, etc.
  - `metaagent-config.json` exists and shows `social: { enabled: false }` plus genesisPrompt.
  - `state.db` SQLite present.
- Runtime produced files unrelated to intended RUNBOOK and exhibited loop detection; wrote e.g. `~/MARKETPLACE_PLAN.md`, `./marketplace_shortlist.md`, `~/marketplace/README.md`, created a skill.
- Next planned fix (tomorrow):
  1) Stop automaton (Ctrl+C).
  2) Determine which config file runtime actually reads for genesis (`automaton.json` vs `metaagent-config.json` vs `config.json`).
  3) Set a tight genesis prompt with an absolute output path (e.g. `/home/seanc/.automaton/RUNBOOK.md`) to avoid `~` expansion issues.
  4) Restart with `node dist/index.js --run`, verify runbook exists + check logs.

- Current status at end of session: `node packages/cli/dist/index.js status` shows `State: sleeping` (loop not running). Recent activity timestamps are from before shutdown.


## 2026-02-21 – Git sync habit
- Daily: before working on any machine, run `cd ~/.openclaw/workspace && git pull --rebase`.
- After making changes (notes, memory, etc.), run `git add . && git commit -m "Update memory + notes" && git push` so both desktop and laptop stay in sync.
