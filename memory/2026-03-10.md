# 2026-03-10

## OpenClaw recovery + tool access
- Root cause of the assistant becoming much less useful was `~/.openclaw/openclaw.json` getting reset to `tools.profile: "messaging"`, which removed local file/shell-style tool access in the live session.
- Fix path that worked: restore `tools.profile: "full"`, keep search config under `tools.web.search.gemini.apiKey`, repair malformed JSON, then restart the gateway.
- `onboard` restored operability when `openclaw gateway restart` was failing/timing out, but it also rewrote config defaults (tool profile, session scope, channels), so it should be treated as a last-resort recovery step, not the normal fix.
- Recovery checklist was written to `docs/openclaw-recovery-checklist.md`.

## Assistant execution preference
- Ethan explicitly wants best-first execution with minimal unnecessary follow-up questions.
- Updated `AGENTS.md`, `USER.md`, and `MEMORY.md` so future sessions default to practical, complete, production-usable answers and do the next obvious internal step when permissions allow.

## Grape pipeline work
- Created `grape_pipeline/` with detector/counter training scripts, inference script, README, and dataset structure.
- Installed a local venv at `grape_pipeline/.venv-grapes` and bootstrapped a first-pass dataset/training loop.
- Local image pool grew to 33 source images in `grape_pipeline/incoming_images`.
- Auto-seeded dataset stats reached 21 usable images and 85 seeded cluster crops (70 train / 15 val).
- Detector training became runnable but still weak; counter training remains unreliable because labels/counts are auto-seeded and noisy.
- Web image collection started via Wikimedia fallback with source manifest under `grape_pipeline/web_sources/wikimedia_candidates/`, but this was throttled by 429s.

## Tomorrow priority
- User wants the website finished tomorrow and does not want another tool/capability regression excuse.
- Before website work, verify OpenClaw is still in the good state: `tools.profile: "full"`, Telegram + WhatsApp present, search configured, local file/shell tools available.
