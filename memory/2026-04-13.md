- Worked with Ethan on two paper-trading bot workstreams in workspace.
- Solana work: current active rebuild is `solana-sniper-v2/`, a DexScreener-based paper bot replacing unreliable CoinGecko Demo tracking. Added filters, stale exits, persisted positions/trades/PnL, startup settings block, and cleaner close/PnL logging. Ethan prefers visible runtime diagnostics and adjustable settings in config.
- Prediction market work: created `prediction-bot-v1/` for Polymarket paper trading. Added market discovery, filtering, paper positions, stale exits, startup settings block, discovery stats, category allow/block lists, and entry rejection summaries.
- Important finding: generic Polymarket Gamma active markets feed used so far does not surface the 5-minute crypto "Up or Down" markets Ethan wants, even though they appear in Polymarket UI screenshots. Next likely step is tracing the actual Polymarket discovery path / endpoint / query used for those short-horizon crypto markets instead of more config tuning.
- Ethan specifically wants short-horizon crypto prediction markets like BTC/ETH up-or-down 5 minute markets, and does not want sports-focused markets.
- Solana bot direction sharpened: Ethan wants discovery focused on tokens that have just graduated from Pump.fun or Bonk/LetsBonk, not generic new Solana tokens.
- `solana-sniper-v2` was extended toward on-chain discovery with `services/solana_discovery.py`, but public Solana RPC (`https://api.mainnet-beta.solana.com`) rejected Raydium `getProgramAccounts` scans for program `675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8` with code `-32010` (`excluded from account secondary indexes`).
- Bot behavior was adjusted so unsupported on-chain RPC discovery falls back immediately to DexScreener discovery, showing `source=onchain->dexscreener_fallback` in logs.
- Runtime logs showed DexScreener fallback producing useful rejection stats, with liquidity as the main current filter bottleneck in at least one sample cycle (`profiles=30`, `seen=7`, `age=6`, `liq=18`).
- Ethan shared relevant Solana bot references: `warp-id/solana-trading-bot` and `hanshaze/solana-sniper-copy-trading-bot`; conclusion was to mine them for discovery architecture ideas but not adopt their live-trading/private-key/executor paths.
- Promising new discovery lead: Ethan has used GMGN (`https://gmgn.ai/ai?chain=sol`) and indicated it helps. Likely next step is tracing GMGN’s actual feed, especially websocket/new-pool style discovery, as a stronger primary source for fresh Pump/Bonk graduates than DexScreener.
