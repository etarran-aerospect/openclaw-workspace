# 2026-03-24

- Ethan confirmed the correct AeroSpect homepage preview baseline is `/home/ethan/.openclaw/workspace/aerospect-homepage-preview-live-baseline/`.
- This is the folder that matches the live-style homepage and contains the `How It Works` section with the text: "From flight to recommendation, we turn aerial data into practical vineyard decisions."
- The target image in that section is `/images/drone-inspection.jpg`.
- Requested homepage preview change remains: replace that single wide image with 3 images: NDVI, Thermal, and RGB with vine overlay; keep everything else unchanged; preview first.
- Wrong folders to avoid for this task: `/home/ethan/.openclaw/workspace/aerospect/` and `/home/ethan/.openclaw/workspace/aerospect-deployed-site/aerospect/`.
- Confirmed again that the correct AeroSpect homepage preview/edit baseline is `/home/ethan/.openclaw/workspace/aerospect-homepage-preview-live-baseline/` and local preview was served on ports 4176/4177 during review.
- Preview edits made in the confirmed baseline bundle (`assets/index-ChH_nNva.js` and CSS bundle) for the `What We Deliver` section only: convert `What We Deliver` to small blue eyebrow text, add headline `Transform Data Into Decisions`, and adjust the 4 feature cards to render 4 across in preview.
- Important implementation note: editing the built preview bundle required direct changes to `assets/index-ChH_nNva.js`; the CSS bundle was also missing utility rules for `grid-cols-4` and `max-w-7xl`, so those were appended directly to `assets/index-CfGdpg2d.css` for the preview to render correctly.
- User feedback on current preview state: 4 cards are now across, but card content/layout still needs refinement because the containers feel too tall and the text does not fill the boxes well enough.

- Ethan wants today's AeroSpect homepage preview state preserved for later deployment work.
- Verified the ZIP downloaded from IONOS (`aerospectinc-website-deployed---4c28c637-6993-48cd-8e36-3474b237939e.zip`) is very likely the current deployed homepage build/output: it contains `index.html` + `assets/index-ChH_nNva.js`, and the JS filename matches the confirmed live-style baseline family.
- The IONOS ZIP does NOT contain today's approved edits yet (for example: `Why Choose Us?`, `Transform Data Into Decisions`, `/images/4 images.png`, `/images/RGB-Overlay.png`).
- Current approved local preview baseline to preserve: `/home/ethan/.openclaw/workspace/aerospect-homepage-preview-live-baseline/`.
- Approved homepage state includes: updated `What We Deliver`; updated `How It Works` text + 3-image layout; `Our Services` restored to original layout with `drone-inspection`; redundant lower section replaced with `Why Choose Us`; donor-style icons patched in preview JS.
- Deployment to IONOS is deferred; next time compare local edited baseline against the IONOS ZIP artifact and determine safe deploy method.

- Confirmed deploy baseline / live-site artifact folder for AeroSpect homepage work: `/home/ethan/.openclaw/workspace/aerospect-homepage-preview-live-baseline/`.
- Ethan and I finalized the homepage preview state in that folder for deployment to IONOS via Webspace Explorer (static artifact style, not source build pipeline).
- Final approved homepage deploy payload is the edited artifact in `aerospect-homepage-preview-live-baseline/` with updated `index.html`, `assets/index-ChH_nNva.js`, `assets/index-CfGdpg2d.css`, and required image uploads under `images/`.
- We explicitly decided NOT to ship the LinkedIn button add-on; homepage should deploy without it.
- Next task after homepage deploy: finalize the sample report page / example analysis page, using the now-confirmed deployed-site artifact family as the baseline.
- Reminder phrase for Ethan: "Homepage lives in `aerospect-homepage-preview-live-baseline`; next up is sample report." 
