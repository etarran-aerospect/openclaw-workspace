
- **Telegram Voice Notes Issue**: Diagnosed why Telegram voice note transcription stopped working. The OpenClaw v2026.4.9 update deprecated the old `voice-call.config.stt` structure. Voice transcription now requires configuring an STT provider (e.g., OpenAI Whisper) under `tools.media.audio.models`.
- **UI Clarification**: Explained to Ethan that `HEARTBEAT_OK` responses are just the background cron system checking for tasks, not a malfunction.
- **Pending Action**: Waiting for Ethan's approval to patch `~/.openclaw/openclaw.json` with the new audio model configuration to restore voice messages.