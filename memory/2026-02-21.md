## 2026-02-21 (PST)

### Shared workspace sync habit (both machines)
Daily sync routine:
- Before working:
  - `cd ~/.openclaw/workspace`
  - `git pull --rebase`
- After making changes (notes, memory, etc.):
  - `git add .`
  - `git commit -m "Update memory + notes"`
  - `git push`

Notes:
- Repo: https://github.com/etarran-aerospect/openclaw-workspace
- Desktop clone remote switched to SSH: `git@github.com:etarran-aerospect/openclaw-workspace.git`
