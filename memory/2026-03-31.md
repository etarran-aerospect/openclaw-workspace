## Vineyard irrigation leak detection work (continued)
- Continued from 2026-03-30 on the vineyard irrigation leak-detection QGIS toolbox.
- Ethan confirmed the leak-detection result should use smaller row sections rather than whole-row polygons, matching the paper’s method.
- Wrote segmented v2 leak tool script to a distinct file to avoid QGIS caching/name confusion:
  - `reports/vineyard_row_leak_detection_tool_v2.py`
- v2 adds parameters and workflow for:
  - `SECTION_LENGTH` (default 2.0 m)
  - `MIN_SECTION_LENGTH`
  - splitting rows into equal-length sections before buffering and scoring
- Confirmed v2 runs successfully when `USE_BLOCK=False` on the Borden thermal example:
  - rows split into equal-length sections
  - non-vine thermal created successfully
  - zonal stats computed successfully
  - section-temperature q20 and field mean reported in QGIS log
  - final section output layer created
- QGIS compatibility issue remains for `USE_BLOCK=True` because Ethan’s build lacks `native:joinattributesbylocationsummary`; workaround is to run v2 with `USE_BLOCK=False` for now, and future patch should replace that dependency with a more portable clip/intersection-based approach.
- Ethan noticed one run only showed sections along the reference row; likely cause was wrong input layer passed into leak tool (reference row instead of generated row-builder output). The segmented v2 run later covered the full block correctly.
- Current interpretation guidance for v2 output:
  - use `delta_mean` as the main anomaly-strength field
  - use `flag` as binary candidate indicator
  - raw `rank_cool` alone is too granular when there are thousands of sections
  - best near-term filtering approach is to keep strong anomalies only, e.g. `"flag" = 1 AND "delta_mean" < -2` (tune threshold as needed)
  - clustered strong sections matter more than isolated single sections
- Ethan wants the process to be visually explainable like the research paper, not just polygons over a map. Next presentation-focused goal:
  - generate intermediate layers / figures showing full thermal, vine-only thermal, non-vine/soil-only thermal, and final flagged row sections so the logic chain is understandable to vineyard managers.
