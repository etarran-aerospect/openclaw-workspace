# 2026-03-12

## Remote setup thread (Ethan)
- Ethan is installing **OpenClaw Browser Relay** extension and asked where to find the **Gateway token**:
  - Location: `~/.openclaw/openclaw.json` → `gateway.auth.token`
- Goal for improving remote use (to do when Ethan is back at Desktop):
  1) Set `GEMINI_API_KEY` for the `openclaw-gateway` **systemd** service (env file + override)
  2) Restart `openclaw-gateway`
  3) Run a quick Gemini ACP smoke test
- Ethan explicitly does **not** want repeating reminders (token burn / tends to get messed up). A repeating 6-hour cron reminder was created then immediately removed.

## Config/auth observations
- Current primary model: `openai-codex/gpt-5.2` via OAuth profile `openai-codex:default`.
- Fallback model configured: `openai/gpt-4o-mini`, but there is no `openai:*` auth profile in config, so provider-level auth fallback likely missing.

## Repo sync
- Ran `git pull --rebase` + `scripts/workspace-sync-check.sh`; workspace clean and up-to-date at `35c593f`.
